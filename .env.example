AZURE_OPENAI_ENDPOINT=https://{your-custom-endpoint}.openai.azure.com/
AZURE_OPENAI_API_KEY={your-api-key}
AZURE_OPENAI_DEPLOYMENT_NAME={your-deployment-name}
AZURE_OPENAI_API_VERSION="{2025-04-01-preview}"

AZURE_OPENAI_PRO_DEPLOYMENT_NAME="{your-deployment-pro-name}"
AZURE_OPENAI_PRO_API_VERSION="{2025-04-01-preview}"
AZURE_OPENAI_PRO_ENDPOINT="https://{your-custom-endpoint}.openai.azure.com/"

AZURE_OPENAI_EMBEDDING_DEPLOYMENT={your-embedding-deployment-name}
AZURE_OPENAI_EMBEDDING_ENDPOINT=https://{your-custom-endpoint}.openai.azure.com/openai/deployments/{your-embedding-deployment-name}/embeddings?api-version=2023-05-15
# Azure AI Search Configuration (replaces ChromaDB)
AZURE_SEARCH_ENDPOINT=https://your-service.search.windows.net
AZURE_SEARCH_API_KEY=your-api-key
AZURE_SEARCH_INDEX_NAME={name}-engineering-diagrams

# Azure Document Intelligence Configuration (for advanced OCR & layout analysis)
# Provides superior text extraction, table parsing, and form recognition for engineering diagrams
# Uses prebuilt-layout model to extract text, tables, and key-value pairs from technical drawings
AZURE_DOCUMENT_INTELLIGENCE_ENDPOINT="https://{your-custom-endpoint}.cognitiveservices.azure.com/"
AZURE_DOCUMENT_INTELLIGENCE_API_KEY="{your-api-key}"

# Azure Blob Storage Configuration (for cloud file input/output)
# Enables reading PDFs/images from blob storage and writing results to blob
# Connection string method (recommended for development):
AZURE_STORAGE_CONNECTION_STRING="DefaultEndpointsProtocol=https;AccountName={your-account};AccountKey={your-key};EndpointSuffix=core.windows.net"

# OR Managed Identity method (recommended for production in Azure):
# AZURE_STORAGE_ACCOUNT_URL=https://{your-account}.blob.core.windows.net
# AZURE_STORAGE_USE_MANAGED_IDENTITY=true

# Default containers
AZURE_STORAGE_INPUT_CONTAINER=engineering-diagrams
AZURE_STORAGE_OUTPUT_CONTAINER=edison-analysis-results

# Optional Performance Settings
MAX_CONCURRENT_PAGES=5
ENABLE_SMART_CHUNKING=true

AZURE_OPENAI_AGENT_ID="{your-agent-id}"
AZURE_OPENAI_AGENT_API_KEY="{your-agent-api-key}"
AZURE_OPENAI_AGENT_PROJECT_ENDPOINT="{your-agent-project-endpoint}"
AZURE_TENANT_ID="{your-tenant-id}"
# Recommended for production in Azure:
AZURE_OPENAI_AGENT_USE_MANAGED_IDENTITY=true

# Bing Search Configuration (Optional - for web search feature)
# IMPORTANT: This is a FULL Azure resource path (with slashes), NOT just the connection name!
# Format: /subscriptions/{subscription_id}/resourceGroups/{resource_group}/providers/Microsoft.CognitiveServices/accounts/{ai_service}/projects/{project_name}/connections/{connection_name}
# 
# How to get this ID:
# 1. Go to https://ai.azure.com
# 2. Open your AI Project
# 3. Click "Connected resources" (left sidebar)
# 4. Add a "Grounding with Bing Search" connection
# 5. Click on the Bing connection to view details
# 6. Copy the full "Connection ID" - it's a long path like the format above
#
# Example (use quotes for paths with slashes):
# BING_CONNECTION_ID="/subscriptions/12345678-abcd-1234-abcd-123456789abc/resourceGroups/my-rg/providers/Microsoft.CognitiveServices/accounts/my-ai-service/projects/my-project/connections/bing-connection"
#
# IMPORTANT: Leave empty or commented out if not using Bing Search (prevents GPT-5 tool errors)
BING_CONNECTION_ID=""